import { resolve } from 'path'

export const SOURCE_PATH = resolve(process.cwd(), 'src')
export const OUTPUT_PATH = resolve(process.cwd(), 'static')
export const PUBLIC_PATH = '/'
export const MANIFEST_NAME = 'manifest.json'