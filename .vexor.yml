language: node_js
node_js: 6.11.0

install:
  - npm i

script:
  - npm run test

cache:
  enabled: true
  directories:
  - ./node_modules

deploy:
  - branch: "development"
    shell:
      - "DEPLOY_TO=humaniq-static OVERRIDE_VALUES=values.yaml ZONE=europe-west4-a ./scripts/deploy.sh"
